import SectionPlaceholder from "@/components/SectionPlaceholder";

export default function VideoPage() {
  return (
    <SectionPlaceholder
      eyebrow="Moving Image"
      title="影像"
      description="这里会收纳短片、纪录、影像实验，以及更适合用画面展开的表达。"
      note="入口先接上，后续往这个栏目里加内容时不用再改导航结构。"
      ctaHref="/issues"
      ctaLabel="先看看本期"
    />
  );
}
