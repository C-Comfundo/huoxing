import SectionPlaceholder from "@/components/SectionPlaceholder";

export default function GamePage() {
  return (
    <SectionPlaceholder
      eyebrow="Play"
      title="游戏"
      description="这里会收纳互动叙事、小型实验和那些需要亲手参与的作品。"
      note="现在先把入口铺好，等内容进来后这一栏就可以直接承接。"
      ctaHref="/"
      ctaLabel="先回首页看看"
    />
  );
}
